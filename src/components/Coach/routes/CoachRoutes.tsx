import React from 'react';
import { Routes, Route } from 'react-router-dom';

export const CoachRoutes = (): JSX.Element => (
    <Routes>
        <Route path="/*" element={<div />} />
    </Routes>
);
